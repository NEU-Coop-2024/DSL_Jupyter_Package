
\usetikzlibrary{positioning, fit, graphs, decorations.pathreplacing}
\begin{tikzpicture}[>=stealth, node distance=2.5cm, scale=1.5, every node/.style={transform shape, draw, circle, minimum size=1.2cm}]
  \node (E) {E};
  \coordinate (prev_level) at (E.south);
  \node[below=of prev_level] (L) {L};
  \coordinate (prev_level) at (L.south);
  \node[right=of L] (O) {O};
  \node[below=of prev_level] (P) {P};
  \coordinate (prev_level) at (P.south);
  \draw[->] (O) -- (P);
  \draw[->] (O) -- (P);
  \draw[->] (O) -- (P);
  \draw[->] (L) -- (O);
  \draw[->] (E) -- (L);
  \draw[->] (E) -- (P);
  \draw[->] (L) -- (P);
\end{tikzpicture}
\end{document}